[project]
name = "decision-intelligence-platform-backend"
version = "0.1.0"
description = "Decision Intelligence Platform Backend"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[dependency-groups]
dev = [
    "pytest",
]