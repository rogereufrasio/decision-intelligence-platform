# Git Rules

Use Conventional Commits.

Commit small logical changes.

Avoid mixing documentation and code in the same commit whenever possible.

Every feature must compile before commit.

Main branch must always remain deployable.